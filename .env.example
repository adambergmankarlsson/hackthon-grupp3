# Copy this file to `assistant/.env`.
# Uses Vertex AI via Application Default Credentials (gcloud) — no API key needed.

GOOGLE_GENAI_USE_VERTEXAI=TRUE
GOOGLE_CLOUD_PROJECT=hackathon2026-c-sbx-557a
GOOGLE_CLOUD_LOCATION=europe-west1
